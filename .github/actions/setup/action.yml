name: 'Setup Workspace'
description: 'Install Node.js, pnpm, dependencies, Python, and PlatformIO. Must be called after actions/checkout.'

runs:
  using: 'composite'
  steps:
    - name: Setup PNPM
      uses: pnpm/action-setup@6e7bdbda5fe05107efc88b23b7ed00aa05f84ca0

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version-file: '.nvmrc'
        cache: 'pnpm'
        registry-url: 'https://registry.npmjs.org'

    - name: Install dependencies
      run: pnpm install --frozen-lockfile
      shell: bash

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'

    - name: Install PlatformIO and ESP tools
      run: |
        python -m pip install --upgrade pip
        pip install platformio esptool
      shell: bash
